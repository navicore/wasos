(myspec) I'm about to work on something and want to establish scope. The work: $ARGUMENTS

1. Read docs/ARCHITECTURE.md, docs/ROADMAP.md, and relevant docs/design/ files (skip docs/design/done/ unless the work relates to a past decision)
2. Identify which files and modules are in scope for this work
3. Identify what is explicitly OUT of scope — what should not be touched
4. List any constraints from the docs that apply
5. If there's a relevant design doc, summarize the approach it prescribes

Output a brief scope statement I can reference during the session. If something seems underspecified, ask me about it rather than assuming.
